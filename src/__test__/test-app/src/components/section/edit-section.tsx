import * as React from "react";
import { ISection } from "../../data/resolvers/initial-data/data.interface";

interface IEditSectionProps {
  section: ISection;
}
export const EditSection = (props: IEditSectionProps) => {
  return null;
};
