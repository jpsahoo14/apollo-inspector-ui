import { editor } from "./editor-resolver";
import { refreshUsers } from "./refresh-users-resolver";
import { users } from "./users-resolver";

export const Query = {
  editor,
  refreshUsers,
  users,
};
